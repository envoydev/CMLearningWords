@model IEnumerable<StageOfMethod>
@{
    List<StageOfMethod> stagesList = (List<StageOfMethod>)Model;
    int count = 1;
}

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="row">
                    <div class="col-md-4">
                        <a asp-controller="StageOfMethod" asp-action="Create" class="btn btn-info">Добавить новый стейдж</a>
                    </div>
                </div>
            </div>
            <div class="panel-body">
                <table class="table" id="tableContent">
                    <thead class="thead-inverse">
                        <tr>
                            <th>#</th>
                            <th>Название</th>
                            <th>Информация</th>
                            <th>Редактировать</th>
                            <th>Удалить</th>
                        </tr>
                    </thead>
                    <tbody id="dataView">
                        @if (stagesList.Count > 0)
                        {
                            for (int i = 0; i < stagesList.Count; i++, count++)
                            {
                                <tr>
                                    <td class="stage">@count</td>
                                    <td class="stage">@stagesList[i].Name</td>

                                    <td class="stage"><a asp-controller="WordInEnglish" asp-action="Details" asp-route-id="@stagesList[i].Id"><span class="glyphicon glyphicon-info-sign"></span></a></td>
                                    <td class="stage"><a asp-controller="WordInEnglish" asp-action="Edit" asp-route-id="@stagesList[i].Id"><span class="glyphicon glyphicon-edit"></span></a></td>
                                    <td class="stage"><a asp-controller="WordInEnglish" asp-action="Delete" asp-route-id="@stagesList[i].Id"><span class="glyphicon glyphicon-trash"></span></a></td>
                                </tr>
                            }
                        }
                        else
                        {
                            @Html.Partial("_NotFound")
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>