<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="row">
                    <div class="col-md-4">
                        <a asp-controller="Item" asp-action="Create" class="btn btn-info">Добавить инвентарь</a>
                    </div>
                    <form method="get">
                        <div class="col-md-4">
                            @*@Html.DropDownList("categoryId", Model.Categories as SelectList, htmlAttributes: new { @class = "form-control" })*@
                        </div>
                        <div class="col-md-4">
                            <div class="input-group">
                                @*@Html.TextBox("itemName", Model.SelectedItemName, htmlAttributes: new { @class = "form-control search-form", @placeholder = "Поиск" })*@
                                <span class="input-group-btn">
                                    <input type="submit" name="submit" class="btn btn-info search-btn" value="Поиск">
                                </span>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            <div class="panel-body">
                <table class="table" id="tableContent">
                    <thead class="thead-inverse">
                        <tr>
                            <th>#</th>
                            <th>Название</th>
                            <th>Уникальный номер</th>
                            <th>Категория</th>
                            <th>Дата добавления</th>
                            <th>Информация</th>
                            <th>Редактировать</th>
                            <th>Удалить</th>
                        </tr>
                    </thead>
                    <tbody id="dataView">
                        @*@if (items.Count > 0)
                        {
                            for (int i = 0; i < items.Count; i++, count++)
                            {
                                <tr>
                                    <th>@count</th>
                                    <th>@items[i].Name</th>
                                    <th>@items[i].InventoryCode</th>
                                    <th>@items[i].Category.Name</th>
                                    <th>@items[i].DateAdding</th>
                                    <th><a asp-controller="Item" asp-action="Details" asp-route-id="@items[i].Id"><span class="glyphicon glyphicon-info-sign"></span></a></th>
                                    <th><a asp-controller="Item" asp-action="Edit" asp-route-id="@items[i].Id"><span class="glyphicon glyphicon-edit"></span></a></th>
                                    <th><a asp-controller="Item" asp-action="Delete" asp-route-id="@items[i].Id"><span class="glyphicon glyphicon-trash"></span></a></th>
                                </tr>
                            }
                        }
                        else
                        {
                            @Html.Partial("_NotFound")
                        }*@
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
